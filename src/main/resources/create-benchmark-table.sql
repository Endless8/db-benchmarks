CREATE TABLE Persons (
    ID int NOT NULL,
    LastName varchar(255) NOT NULL,
    FirstName varchar(255) NOT NULL,
    Age int NOT NULL,
    BirthDate date NOT NULL,
    PersonalUUID varchar(255) NOT NULL,
    PRIMARY KEY (ID)
);